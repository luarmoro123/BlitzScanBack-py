# ═══════════════════════════════════════════════════
# BlitzScan Backend — Plantilla de Variables de Entorno
# ═══════════════════════════════════════════════════
# Copia este archivo como .env y completa los valores:
#   cp .env.template .env
# ═══════════════════════════════════════════════════

PROJECT_NAME=BlitzScan Backend

# Genera una clave secreta aleatoria (nunca compartas esta):
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CAMBIAR_POR_UNA_CLAVE_ALEATORIA

# Base de datos PostgreSQL (Neon o local)
DATABASE_URL=postgresql+asyncpg://usuario:contraseña@host/basedatos?ssl=require

# Orígenes permitidos para CORS (separados por coma)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Redis para Celery (cola de tareas async)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
